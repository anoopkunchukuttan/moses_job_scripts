Standardize_corpus.py -F <path>

Split the corpus into three parts by default (20% test,5% tune and rest train)

For more help
Standardize_corpus.py --help

Merger is a tool to merge files in a directory.

Hard-coded many things. Will fine tune codes once I am back.


ILCI data is inconsistent. I have put it in Source_Corpus.

Folder parallel_corpus is ready to be given as an input to moses.

